import {requester} from "../lib/requester";

/**
 * Unload the trained model
 * @param request
 * @param response
 * @returns {Promise<void>}
 */
/*const deleteModelRasa = async (request, response) => {
  const result = await requester('delete', request.get('project'), {model:request.get('model')});
  response.setHeader('Content-Type', 'application/json');
  response.send(result);
}

module.exports = {
  deleteModelRasa
}
*/
